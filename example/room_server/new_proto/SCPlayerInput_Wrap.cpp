#include "proto/all_wraps.hpp"

namespace CytxGame
{
    REGISTER_PROTOCOL(SCPlayerInput_Wrap);

    void SCPlayerInput_Wrap::process(msg_ptr& msgp, connection_ptr& conn_ptr, game_server_t& server)
    {

    }
}
